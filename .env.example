LOOMTABLE_POSTGRES_PASSWORD=change-me
# Native default and Compose host publication: 127.0.0.1:31201
# Hash a token with SHA-256 before placing it here.
LOOMTABLE_AUTH_TOKEN_HASH=replace-with-token-sha256
